#include <iostream>
#include <string>
using namespace std;
int main() {
    string p;
    cout<<"Palabra: ";
    cin>>p;
    string r = string(p.rbegin(), p.rend());
    if(p==r) cout<<"Es palindromo";
    else cout<<"No lo es";
}
